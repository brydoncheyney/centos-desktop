---
- hosts: all
  become: yes
  gather_facts: yes

  roles:
    - user-accounts
    - docker
    - {role: git, tags: ['git']}
    - google-chrome
    - vagrant
    - zsh
    - ohmyzsh
    - keybindings
