---
- name: system wide configuration
  hosts: all
  become: yes
  gather_facts: yes

  roles:
    - docker
    - google-chrome
    - vagrant
    - zsh

- name: user configuration
  hosts: all
  become: yes
  become_user: vagrant
  gather_facts: yes

  roles:
    - ohmyzsh
