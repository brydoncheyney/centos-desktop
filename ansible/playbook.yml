---
- hosts: all
  become: yes
  gather_facts: yes

  roles:
    - user-accounts
    - docker
    - {role: git, tags: ['git']}
    - {role: ruby, tags: ['ruby']}
    - {role: java, tags: ['java']}
    - google-chrome
    - vagrant
    - zsh
    - ohmyzsh
    - keybindings
